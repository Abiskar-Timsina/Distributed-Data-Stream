apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.pvc.name }}
spec:
  capacity:
    storage: {{ .Values.pvc.size }}
  accessModes:
    - {{ .Values.pvc.accessMode }}
  persistentVolumeReclaimPolicy: Retain

  # local: We'd rather use the `hostPath` instead of `local` for this 
  # particular PersistentVolume, since, I'd rather store the files 
  # in my host Device instead of putting strain on minikube's 
  # storage provisioner
  hostPath:
    path: {{ .Values.pvc.mountPath }}

  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ .Values.pvc.nodeName }} # We need to make sure we're using pvc within a specified node.
