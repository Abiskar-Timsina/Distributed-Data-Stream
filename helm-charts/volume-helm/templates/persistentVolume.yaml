apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.pv.name }}
spec:
  capacity:
    storage: {{ .Values.pv.size }}
  accessModes:
    - {{ .Values.pv.accessMode }}
  persistentVolumeReclaimPolicy: Retain

  # local: We'd rather use the `hostPath` instead of `local` for this 
  # particular PersistentVolume, since, I'd rather store the files 
  # in my host Device instead of putting strain on minikube's 
  # storage provisioner
  hostPath:
    path: {{ .Values.pv.mountPath }}

  storageClassName: {{ .Values.pv.storageClassName }}

  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ .Values.pv.nodeName }} # We need to make sure we're using pv within a specified node.
